import React from "react";
import Projects from "../Components/Projects/Projects";

export const metadata = {
  title: "MSR | Projects",
};

const page = () => {
  return (
    <div>
      <Projects />
    </div>
  );
};

export default page;
