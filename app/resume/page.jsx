import React from "react";
import Resume from "../Components/Resume/Resume";

export const metadata = {
  title: "MSR | Resume",
};

const page = () => {
  return (
    <div>
      <Resume />
    </div>
  );
};

export default page;
